<!--
part_b.md — Part B subsections (chipcode / firmware).

Same format as part_a.md; headings are numbered 3.1, 3.2, ...

This file is legitimately empty most of the time. If no firmware image changed,
the Part B change table already states that per image, each pinned to the
commit it sits at — no prose needed. Only add sections here when an image
actually moved and the reason matters.
-->
